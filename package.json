{
  "name": "library-app",
  "version": "1.0.0",
  "private": true,
  "description": "Knihovna — správa autorů a knih (UI + REST API)",
  "main": "app.js",
  "scripts": {
    "postinstall": "npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run build && NODE_ENV=production node app.js",
    "dev": "NODE_ENV=development node app.js"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "express": "^4.18.2",
    "uuid": "^9.0.0"
  }
}
